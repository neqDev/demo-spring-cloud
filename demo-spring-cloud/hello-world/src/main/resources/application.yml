
spring:
  application:
    name: hello-world
  config:
    import:
      - configserver:http://localhost:8888
  cloud:
    config:
      label: main
      fail-fast: true
server:
  port: 9998
eureka:
  instance:
    prefer-ip-address: true